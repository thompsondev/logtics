// Auth layout — login / register pages.
// No sidebar. Inherits root <html>/<body> and Providers.
export default function AuthLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
